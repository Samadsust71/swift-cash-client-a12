import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
    {
      path: "/",
      element: <div className=" text-brand-primary">Hello world!</div>,
    },
  ]);

export default router  